Play button can pause or play depending on which is happening
Block button stops the song and ends loops
Forward and Rewind forwards or rewinds based on the skip time.
Forward can skip to next song based on how much time has elapsed.
Menu button shines green when hovered over, opens a menu,
+1 button adds a loop
infinity button makes it loop infinitely
Light bulb button changes the colour of the music player
moon button also changes the colours
the switch button changes how much time is skipped when skipping.
the empty button ( i did not put an image innit) turns on random mode, which makes the skip button go to a random song.
volume button on the top left can mute/unmute the songs.
Button on the top right shows a list of songs that are in the playlist..
timer changes based on looping.
progress bar shows only the progress of a song.